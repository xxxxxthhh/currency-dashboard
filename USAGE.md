# 汇率监控 Dashboard - 使用说明

## 📖 快速开始指南

### 1. 本地预览

在项目目录下运行：

```bash
cd /workspace/group/currency-dashboard
python3 -m http.server 8000
```

然后在浏览器打开：`http://localhost:8000`

### 2. 部署到 GitHub Pages

#### 步骤 1：创建 GitHub 仓库

```bash
cd /workspace/group/currency-dashboard
git init
git add .
git commit -m "Initial commit: Currency Dashboard"
git branch -M main
git remote add origin https://github.com/你的用户名/currency-dashboard.git
git push -u origin main
```

#### 步骤 2：启用 GitHub Pages

1. 进入仓库的 Settings
2. 找到 Pages 选项
3. Source 选择 `main` 分支
4. 点击 Save
5. 等待几分钟后访问：`https://你的用户名.github.io/currency-dashboard/`

### 3. 自动更新数据

GitHub Actions 已配置为每天自动运行，更新汇率数据。

手动触发更新：
1. 进入仓库的 Actions 标签
2. 选择 "Update Currency Data"
3. 点击 "Run workflow"

## 🎯 功能使用

### 查看实时汇率

Dashboard 顶部显示 4 个货币对的实时汇率卡片：
- **蓝色**：正常（偏离 < 1.5σ）
- **黄色**：警告（1.5σ ≤ 偏离 < 2σ）
- **红色闪烁**：警报（偏离 ≥ 2σ）

### 切换货币对

使用"选择货币对"下拉菜单，切换查看不同货币对的详细图表。

### 调整时间范围

使用"时间范围"下拉菜单，选择：
- 30天
- 90天
- 180天
- 1年

### 理解图表

图表显示：
- **蓝色实线**：实际汇率走势
- **绿色虚线**：均值
- **橙色虚线**：均值 ± 1σ（标准差）
- **红色虚线**：均值 ± 2σ

当汇率突破这些线时，说明出现异常波动。

## 🔔 预警系统

### 自动预警

系统已设置每天凌晨 1:00（北京时间 09:00）自动检查汇率偏离情况。

如果检测到异常，会通过 WhatsApp 发送消息。

### 手动检查

运行预警脚本：

```bash
cd /workspace/group/currency-dashboard
python3 check_alerts.py
```

## ⚙️ 自定义配置

### 修改预警阈值

编辑 `check_alerts.py`：

```python
ALERT_THRESHOLD_SIGMA = 2.0    # 红色预警阈值
WARNING_THRESHOLD_SIGMA = 1.5  # 黄色预警阈值
```

### 添加新货币

1. 编辑 `js/main.js`，在 `updateStatCards` 函数中添加货币代码
2. 编辑 `index.html`，添加对应的统计卡片
3. 更新 `fetch_data.py` 中的 `CURRENCIES` 列表

### 修改检查频率

编辑 `.github/workflows/update-data.yml`：

```yaml
schedule:
  - cron: '0 0 * * *'  # 修改为你想要的时间
```

## 🐛 故障排除

### Dashboard 无法加载数据

检查：
1. `data/historical.json` 文件是否存在
2. 浏览器控制台是否有错误信息
3. 确保使用 HTTP 服务器访问（不是直接打开 HTML 文件）

### GitHub Actions 失败

检查：
1. 仓库是否有写入权限
2. Python 脚本是否有语法错误
3. 查看 Actions 日志获取详细错误信息

### 预警未发送

检查：
1. 定时任务是否正常运行
2. 是否真的有汇率偏离超过阈值
3. WhatsApp 集成是否配置正确

## 📞 获取帮助

如有问题，请：
1. 查看 README.md
2. 检查浏览器控制台错误
3. 查看 GitHub Actions 日志
4. 联系开发者

---

**提示**：当前使用模拟数据。实际部署时需要集成真实的汇率 API。
